---
title: "34. Data Files"
description: Ship config as XML and CSV instead of clicking it into existence, learn exactly what noupdate protects, and translate the module into French.
---

**Goal:** ship a service type as proper master data instead of a UI click, build a
small self-contained demo dataset with `ref()` across files, find out exactly
what `noupdate` does and does not protect, and translate both halves of the module into
French. · **Time:** ~2.5 h · **Checkpoint:** `code/checkpoints/ch34`

## Why this matters

Back in chapter 11 you created three service types by clicking "New" in the list
view. That was the right call at the time, you were learning views, not data files.
But it does not scale: a fresh install of LibreFleet should not depend on someone
remembering to click New three times in the right order. `ir_sequence.xml` (chapter
14) and `mail_template.xml` (chapter 33) already shipped two pieces of config as data
files, both promising "chapter 34 goes deeper on `noupdate`." This is that chapter:
where XML earns its place over CSV, what a module's `demo` key actually does (and
when it very deliberately does nothing), and the exact rule that decides whether an
upgrade overwrites a record or leaves it alone.

The objection to get out of the way first: you have already made these records by
clicking, so writing them again in a file feels like doing the same work twice. It is
not the same work. **A record you click exists in one database, yours. A record in a
data file exists in every database that ever installs the module.** The file is the
definition; the row in Postgres is a copy of it that Odoo makes on your behalf, once
per install, forever. That reversal is the whole chapter: from here on the file is the
thing you maintain, and the database is downstream of it.

## Concepts

Five ideas, and they answer five separate questions. Which format do I write, XML or CSV?
Does this record ship to every install or only to demos? On the next upgrade, does my file
overwrite what is in the database or leave it alone? How do records in different files
point at each other? And what happens to all of it in a second language? The third one,
`noupdate`, is the one people get wrong, so it gets the most room.

### XML and CSV are the same loader, two shapes

Every data file, XML or CSV, ends up creating or updating records through the same
mechanism: a Python `<record>`-equivalent that resolves an xml id, builds a `vals`
dict from fields, and calls `create` or `write`. The format you pick is about shape,
not capability.

Your own module already has one of each. `security/ir.model.access.csv`:

```console
$ head -3 addons/librefleet/security/ir.model.access.csv
```

```text
id,name,model_id:id,group_id:id,perm_read,perm_write,perm_create,perm_unlink
access_librefleet_vehicle_user,librefleet.vehicle user,model_librefleet_vehicle,group_librefleet_user,1,1,0,0
access_librefleet_vehicle_manager,librefleet.vehicle manager,model_librefleet_vehicle,group_librefleet_manager,1,1,1,1
```

Sixteen rows, all with the same six fields. CSV is that: a uniform table, one row
per record, nothing that varies in shape from row to row. `data/mail_template.xml`
is the opposite case, one record with a nested HTML field and nothing else like it
in the file: XML is for records whose shape isn't a table, or where there is only
one or two of them and a header row would be overkill.

<Callout type="info" title="Official docs">
  The [data files reference (19.0)](https://www.odoo.com/documentation/19.0/developer/reference/backend/data.html)
  covers the full `<record>`/`<field>` grammar this chapter builds on: `eval`,
  `search`, function calls, and more than the handful of attributes used here.
</Callout>

### A CSV file's name is not a label, it's the model

The single fact that will burn you if you skip it: **the filename before `.csv` is
the model's technical name**, not a description you chose. Odoo's loader takes the
part of the filename before the first `-` and looks up a model with that exact
name. `ir.model.access.csv` works because `ir.model.access` is a real model. Need a
second data file for the same model, or a descriptive suffix? Add a dash: core's
Kuwait localization ships `account.account-kw.csv`, model `account.account`, suffix
`kw`.

The chapter's own transcript, first attempt, named more like a description than a
model:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial_demo_test2 -i librefleet --with-demo --stop-after-init
```

```text
Module librefleet: loading demo
loading librefleet/data/res_partner_demo.csv
Module librefleet demo data failed to install, installed without demo data
KeyError: 'res_partner_demo'
```

`res_partner_demo.csv` has no dash, so the whole basename becomes the model lookup:
a model literally named `res_partner_demo`, which does not exist. Renaming it
`res.partner-demo.csv` (model `res.partner`, suffix `demo`) fixed it outright, no
other change needed.

### Master data vs demo data: same mechanism, opposite intent

Two manifest keys load data files, and the difference is not the file format:

- **`"data"`**: everything the module needs to be useful. Runs on every install and
  every upgrade.
- **`"demo"`**: fictional records for training, demoing, and testing. Runs only
  when demo is explicitly requested.

The `service_type_master.xml` you are about to write is master data: a real,
required service type. The customer, vehicle, and order you'll add after it are
demo data: illustrative, disposable, never something a production install needs.

### `noupdate`: what it protects, and the one thing it never claimed to

Before the loader source, the question `noupdate` exists to answer. Your file says the fee
is 59. A manager has since changed it to 75 in the UI. You upgrade the module. **Who wins,
the file or the database?**

- **Without `noupdate`** (the default for a `"data"` XML file), the **file wins**. Every
  upgrade rewrites the record to match the file exactly, and the manager's 75 is gone.
- **With `noupdate`**, the **database wins**. The record is left exactly as it is, even
  though the file in front of you says something different.

That is the whole idea, and the choice is about ownership: config *you* own (a sequence
prefix, a technical setting) should let the file win, so upgrades can correct it. Config
*the customer* owns (prices, email wording, anything a manager is invited to edit) needs
`noupdate`, or every upgrade silently undoes their work.

The catch, and the thing `noupdate` never promised, is in the next paragraph: "the database
wins" only applies to records that *already exist*.

Chapter 14 wrapped the sequence in `<data noupdate="1">` and promised more detail
here. Reading Odoo 19's own loader (`odoo/modules/loading.py`,
`odoo/tools/convert.py`) settles it precisely instead of by folklore:

```python
convert_file(env, package.name, filename, idref, mode, noupdate=kind == 'demo')
```

Every file listed under `"demo"` loads with `noupdate=True` automatically,
regardless of format. A `"data"`-listed **XML** file defaults to `noupdate=False`,
but a `<data noupdate="1">` block inside it can raise that for just its own
records, exactly what `ir_sequence.xml` and `mail_template.xml` do. A **CSV** file
has no such block to write, so a `"data"`-listed CSV can never become noupdate, and
a `"demo"`-listed CSV can never opt out of it. The manifest list you put a file in
decides its default; only XML can override that default per file.

What noupdate actually does on an upgrade, from `_tag_record` in the same file:

```python
if self.noupdate and self.mode != 'init':
    if record := env['ir.model.data']._load_xmlid(xid):
        # if the resource already exists, don't update it but store
        # its database id (can be useful)
        self.idref[xid] = record.id
        return None
    # else create it normally
```

Two branches, and only the first one is "protection": if a record with this
exact xml id **already exists**, its field values are left untouched, full stop,
even if the XML in front of you now says something different. But if the xml id
**does not exist yet**, the record is created normally, noupdate or not. `noupdate`
protects existing records from being silently rewritten. It has never claimed to
block new records from a later upgrade, which is exactly what lets you add a second
master-data record in a follow-up release without touching the first.

<Mermaid
  chart={`flowchart TD
    A["module upgrade runs"] --> B{"file's noupdate?<br/>(demo key: always true,<br/>data key: only inside<br/>a data noupdate='1' block)"}
    B -- "false" --> C["record is written every time,<br/>matching the file exactly"]
    B -- "true" --> D{"xml id already<br/>exists in this db?"}
    D -- "yes" --> E["left alone: existing<br/>values are never touched"]
    D -- "no" --> F["created normally,<br/>same as any other record"]`}
/>

### `ref()` and xml ids: one namespace, any format

First, what an xml id is for, because the name says nothing useful. A record's real
database id is a number Postgres hands out, and that number is **different in every
install**: your Tire Rotation might be id 4, a customer's might be id 91. So a data file
can never point at another record by number. An **xml id is a permanent nickname** you
choose, stored next to the record in `ir.model.data`, and it is the same in every database
forever. That is the only reason files can reference each other at all.

Every record loaded from any data file, XML or CSV, register or row, gets an xml
id in `ir.model.data`, and that id is addressable from anywhere in the same
namespace regardless of which file or format originally created it. XML spells a
lookup as `ref="xmlid"` on a `<field>`, or `ref('xmlid')` in a Python `eval`; CSV
spells the same lookup as a `field_name:id` column holding the xmlid string. Same
module, bare id (`vehicle_demo_ag_12_345`); another module, dotted
(`base.group_user`, already in your own `security/librefleet_security.xml` from
chapter 10). The demo data below chains all three: a CSV row references another
CSV row's id, and an XML record references both a CSV row's id and a master-data
XML record's id, four files deep, one shared namespace.

### Translations are data files too, in two very different places

A `.po` file is a data file, which is why it belongs in this chapter. But translations
split into two halves that are stored nothing alike, and knowing which half you are
looking at saves a lot of confusion.

| | What it covers | Marked with | Stored in |
|---|---|---|---|
| **Code strings** | literals in Python and XML: error messages, button labels | `_("...")` | the module's `i18n/*.po` on disk, cached in memory |
| **Field values** | data a user typed or you shipped: a service type's name | `translate=True` on the field | the database, as a `jsonb` column |

Read the right-hand column again, because it is the part that surprises people:
**code translations never enter the database at all.** They are read from your module's
`.po` files and held in memory. Field translations are the opposite: they live in
Postgres, one JSON key per language, in the very same column as the original.

<Callout type="warn" title="ir.translation is gone">
  If you have read older Odoo tutorials, you know a model called `ir.translation` that
  held every translated term in one big table. It does not exist in Odoo 19:
  `"ir.translation" in env` is `False`. Field values moved into `jsonb` columns and code
  strings moved into the in-memory catalog above. Any answer that tells you to query or
  write `ir.translation` was written for Odoo 15 or earlier.
</Callout>

`translate=True` is the interesting one for a data-files chapter, because turning it on
**changes the column type**. Chapter 21 warned you that a translatable field is JSON in
Postgres, using a core product as the example. Below you do it to your own model and
watch the migration happen.

### The i18n CLI, which moved in Odoo 19

Translations get their own subcommand, alongside `db` and `shell` from chapter 6:

```console
$ docker compose exec odoo odoo i18n --help
usage: odoo [--addons-path=PATH,...] i18n [-h] {import,export,loadlang} ...
```

Three verbs: `loadlang` installs a language, `export` writes a `.po`/`.pot` of your
module's terms, `import` reads one back in. Older tutorials use server flags instead
(`--load-language`, `--i18n-export`, `--i18n-import`); on 19 the subcommand is the way in.

## Hands-on

<Steps>

<Step>
### Ship a service type as master data, not a UI click

Create `data/service_type_master.xml`:

```xml title="addons/librefleet/data/service_type_master.xml"
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Master data: config the app needs to be useful out of the box, not
         examples to delete before going live. noupdate="1" so a manager's
         edits (say, a price change) survive the next module upgrade. -->
    <data noupdate="1">
        <record id="service_type_tire_rotation" model="librefleet.service.type">
            <field name="name">Tire Rotation</field>
            <field name="flat_fee">59.0</field>
            <field name="default_duration_h">0.5</field>
        </record>
    </data>
</odoo>
```

Register it in the manifest, after the existing data files and before the views:

```python
    "data": [
        "security/librefleet_security.xml",
        "security/ir.model.access.csv",
        "data/ir_sequence.xml",
        "data/mail_template.xml",
        "data/service_type_master.xml",
        "views/vehicle_views.xml",
        ...
```

Bump the manifest to `19.0.1.19.0` and upgrade:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
...
INFO tutorial odoo.modules.loading: loading librefleet/data/service_type_master.xml
INFO tutorial odoo.modules.loading: Module librefleet loaded in 0.29s, 526 queries (+526 other)
```

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select name, flat_fee from librefleet_service_type where name = 'Tire Rotation';"
```

```text
     name      | flat_fee
---------------+----------
 Tire Rotation |       59
(1 row)
```

Shipped, alongside your three hand-clicked ones from chapter 11, untouched.

</Step>

<Step>
### Prove noupdate, both halves, on your own data

First, try to "fix" the price and see nothing happen. Edit the record you just
wrote, bumping the fee, and add a second record in the same block:

```xml
        <record id="service_type_tire_rotation" model="librefleet.service.type">
            <field name="name">Tire Rotation</field>
            <field name="flat_fee">75.0</field>
            <field name="default_duration_h">0.5</field>
        </record>
        <record id="service_type_wheel_alignment" model="librefleet.service.type">
            <field name="name">Wheel Alignment</field>
            <field name="flat_fee">45.0</field>
            <field name="default_duration_h">0.75</field>
        </record>
```

Upgrade again, then check both:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
$ docker compose exec db psql -U odoo -d tutorial -c "select name, flat_fee from librefleet_service_type where name in ('Tire Rotation', 'Wheel Alignment');"
```

```text
      name       | flat_fee
-----------------+----------
 Tire Rotation   |       59
 Wheel Alignment |       45
(2 rows)
```

Tire Rotation's flat_fee is still 59, the upgrade you just ran did not touch it,
exactly as the loader code in Concepts said it wouldn't. Wheel Alignment, a
brand-new xml id, was created normally in that same upgrade.

There is "who wins" settled by experiment, and both answers in one upgrade. For the record
that already existed, the **database won**: your file says 75 and Postgres still says 59.
For the xml id that did not exist yet, there was nothing to protect, so the **file won** and
the record appeared. `noupdate` guards records, not files.

Now put the price back to 59 in the XML, so the source file stops lying about what's
actually in your database (it won't change anything in Postgres either way, but a chapter's
source should say what's true):

```xml
            <field name="flat_fee">59.0</field>
```

</Step>

<Step>
### Build a small, self-contained demo dataset

Three files, chained by `ref()`, none of them touching anything you already
created by hand. A demo customer, CSV:

```text title="addons/librefleet/data/res.partner-demo.csv"
id,name
partner_demo_nora_baumann,Nora Baumann
```

A demo vehicle, CSV, its `owner_id:id` column pointing at the row above by xml id:

```text title="addons/librefleet/data/librefleet.vehicle-demo.csv"
id,license_plate,model_name,year,mileage_km,owner_id:id
vehicle_demo_ag_12_345,AG 12 345,Citroen Berlingo,2021,38000,partner_demo_nora_baumann
```

A demo service order, XML (it needs `ref()`, which CSV cannot do, and there's only
one record, no table to speak of), pointing at the vehicle above and at the Tire
Rotation master data from step 1:

```xml title="addons/librefleet/data/service_order_demo.xml"
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Demo data: illustrative, not required. The "demo" manifest key makes
         Odoo load this with noupdate=1 automatically, the same protection
         data/service_type_master.xml asks for explicitly, see chapter 34. -->
    <record id="service_order_demo_ag_12_345" model="librefleet.service.order">
        <field name="vehicle_id" ref="vehicle_demo_ag_12_345"/>
        <field name="service_type_id" ref="service_type_tire_rotation"/>
        <field name="scheduled_start" eval="'2026-08-10 08:00:00'"/>
        <field name="scheduled_end" eval="'2026-08-10 08:30:00'"/>
    </record>
</odoo>
```

Register all three under a new manifest key, in dependency order (the vehicle
needs the partner to exist first, the order needs the vehicle):

```python
    "demo": [
        "data/res.partner-demo.csv",
        "data/librefleet.vehicle-demo.csv",
        "data/service_order_demo.xml",
    ],
```

</Step>

<Step>
### Try the obvious thing, watch it do nothing, find out why

Upgrade your real tutorial database exactly like every other chapter so far:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
```

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select count(*) from res_partner where name='Nora Baumann';"
```

```text
 count
-------
     0
```

Nothing. Not an error, not a warning, just silently absent, and the log from that
upgrade never even mentions the three new files. The reason lives in
`ir_module_module`:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select demo from ir_module_module where name='librefleet';"
```

```text
 demo
------
 f
```

Odoo remembers, per module, whether demo was ever loaded for it, in that one
column. Chapter 8 installed librefleet with plain `-i librefleet`, no demo flag,
and back then the module had no `demo` key at all, so `demo` was set to `false` and
persisted. Reading `odoo/modules/loading.py`'s upgrade branch shows why that
persisted value is the only thing that matters now:

```python
else:  # 'upgrade' or 'reinit'
    load_data(env, idref, mode, kind='data', package=package)
    if package.demo:
        package.demo = load_demo(env, package, idref, mode)
```

An upgrade checks `package.demo`, the value **already sitting in the database**,
not whether your manifest now has demo files. Install-time is the only moment that
flag can flip from false to true, and adding demo files months later does not
retroactively flip it.

There's a second, version-specific reason install-time itself needs an explicit
ask. Checking the actual flag rather than trusting an old habit:

```console
$ docker compose exec odoo odoo server --help | grep -A2 without-demo
```

```text
    --without-demo=BOOL
                        don't install demo data in new databases (default)
    --skip-auto-install
```

Odoo 19 installs **without** demo unless you ask for `--with-demo` explicitly. If
you remember older tutorials where demo just showed up, that default is why this
one doesn't match your memory.

</Step>

<Step>
### See it load for real, in a database built to be thrown away

The only way to install-with-demo is a database that has never installed
librefleet before, so build a scratch one:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial_demo_check -i librefleet --with-demo --stop-after-init
...
INFO tutorial_demo_check odoo.modules.loading: Module librefleet: loading demo
INFO tutorial_demo_check odoo.modules.loading: loading librefleet/data/res.partner-demo.csv
INFO tutorial_demo_check odoo.modules.loading: loading librefleet/data/librefleet.vehicle-demo.csv
INFO tutorial_demo_check odoo.modules.loading: loading librefleet/data/service_order_demo.xml
```

```console
$ docker compose exec db psql -U odoo -d tutorial_demo_check -c "select so.reference, st.name, v.license_plate, p.name from librefleet_service_order so join librefleet_service_type st on st.id=so.service_type_id join librefleet_vehicle v on v.id=so.vehicle_id join res_partner p on p.id=v.owner_id;"
```

```text
  reference   |     name      | license_plate |     name
--------------+---------------+---------------+--------------
 SO/2026/0001 | Tire Rotation | AG 12 345     | Nora Baumann
(1 row)
```

Every ref resolved: the order to the vehicle, the vehicle to its owner, the order
to the master-data service type from step 1, four files and two formats, one
namespace. Even the sequence from chapter 14 ran normally against a record it had
never seen before. Once you've confirmed it, drop the scratch database; it did its
job.

```console
$ docker compose exec db psql -U odoo -d postgres -c "drop database tutorial_demo_check;"
```

</Step>

<Step>
### Make a field translatable, and watch its column change type

Service type names are shown to users, so a workshop with French staff wants them
translated. That is one keyword in `models/service_type.py`:

```python title="addons/librefleet/models/service_type.py"
    name = fields.Char(required=True, translate=True)
```

Before you upgrade, note what the column is today:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select data_type from information_schema.columns where table_name='librefleet_service_type' and column_name='name';"
```

```text
     data_type
-------------------
 character varying
```

Bump the manifest to `19.0.1.20.0`, upgrade, and look again:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
$ docker compose exec db psql -U odoo -d tutorial -c "select data_type from information_schema.columns where table_name='librefleet_service_type' and column_name='name';"
```

```text
 data_type
-----------
 jsonb
```

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select name, flat_fee from librefleet_service_type order by id;"
```

```text
             name              | flat_fee
-------------------------------+----------
 {"en_US": "Oil change"}       |       89
 {"en_US": "Brake inspection"} |      120
 {"en_US": "Full service"}     |      349
 {"en_US": "Tire Rotation"}    |       59
 {"en_US": "Wheel Alignment"}  |       45
```

Odoo rewrote the column *and* moved every existing value under an `en_US` key, including
the three you hand-clicked back in chapter 11. No migration script, no data loss. This is
the single best demonstration in the book of what an upgrade will do for you when a field
definition changes underneath existing rows.

It also means every psql query earlier in this chapter that touched that column reads
differently from here on. Step 5's join now shows `{"en_US": "Tire Rotation"}` where it
used to show `Tire Rotation`, and step 1's filter stops working outright, which is worth
seeing rather than reading:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select name, flat_fee from librefleet_service_type where name = 'Tire Rotation';"
```

```text
ERROR:  invalid input syntax for type json
LINE 1: ...lat_fee from librefleet_service_type where name = 'Tire Rota...
DETAIL:  Token "Tire" is invalid.
```

Postgres is trying to parse `Tire Rotation` as JSON. Reach into the key instead:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select name->>'en_US' as name, flat_fee from librefleet_service_type where name->>'en_US' = 'Tire Rotation';"
```

```text
     name      | flat_fee
---------------+----------
 Tire Rotation |       59
```

The ORM needs none of this: a domain like `[("name", "=", "Tire Rotation")]` keeps working
untouched, which is why this chapter's own checks did not change. Only raw SQL has to know.

</Step>

<Step>
### Mark your code strings for translation

`translate=True` covers data. Literals in your Python are the other half, and they need
wrapping in `_()`. The wizard from chapter 20 has two user-facing messages, so open
`wizards/service_order_approve.py` and wrap both:

```python title="addons/librefleet/wizards/service_order_approve.py"
from odoo import _, fields, models
```

```python
        if order.stage != "in_progress":
            raise UserError(
                _("Only an order that is in progress can be marked done."))
        if order.margin < 0 and not self.override_negative_margin:
            raise UserError(
                _("This order loses money (margin %(margin).2f). A manager must "
                  "tick the override to complete it anyway.", margin=order.margin))
```

Two things about that second one. The interpolation moved *inside* `_()` and became a
**named** placeholder, `%(margin).2f` with `margin=` passed as a keyword. Formatting
outside the call (`_("...") % value`) would hand translators a string with a bare `%.2f`
in it and no clue what it holds; a named placeholder survives translation and can even be
reordered by the translator. The output is identical, `-210.90` either way, so chapter
20's transcript still reads exactly as it did.

<Callout type="info" title="Two spellings of the same helper">
  `from odoo import _` is what you will meet almost everywhere: 990 files under
  `addons/*/models/` use it in Odoo 19, against 238 for the newer `self.env._("...")`.
  Both work and mean the same thing. This book uses the import form because that is what
  core mostly still looks like.
</Callout>

</Step>

<Step>
### Install French

Install French. Pass the **iso_code**, `fr`, not the locale code `fr_FR`:

```console
$ docker compose exec odoo odoo i18n loadlang -c /etc/odoo/odoo.conf -d tutorial -l fr
```

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select code, iso_code from res_lang where active order by code;"
```

```text
 code  | iso_code
-------+----------
 en_US | en
 fr_FR | fr
```

Loading a language also fills in every translation Odoo already ships, which you can see
in any core translatable column, both languages in one row:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select name from res_country where id=1;"
```

```text
                   name
------------------------------------------
 {"en_US": "Andorra", "fr_FR": "Andorre"}
```

</Step>

<Step>
### Export what you just marked

Now export your own module's terms as a template. The `.pot` is the untranslated master
list a translator works from:

```console
$ docker compose exec -T odoo odoo i18n export -c /etc/odoo/odoo.conf -d tutorial librefleet -l pot -o - > addons/librefleet/i18n/librefleet.pot
$ wc -l addons/librefleet/i18n/librefleet.pot
```

```text
869 addons/librefleet/i18n/librefleet.pot
```

146 terms, and the interesting part is *where they came from*. Three different kinds of
entry, each with a comment saying its source:

```console
$ grep -B2 -A1 "Only an order that is in progress" addons/librefleet/i18n/librefleet.pot
```

```text
#. odoo-python
#: code:addons/librefleet/wizards/service_order_approve.py:0
msgid "Only an order that is in progress can be marked done."
msgstr ""
```

That one is there **because you wrapped it in `_()` in step 7**, and it points back at the
exact file. The second kind needs no marking at all, because Odoo knows your field labels
already:

```text
#. module: librefleet
#: model:ir.model.fields,field_description:librefleet.field_librefleet_service_type__default_duration_h
msgid "Default Duration (h)"
msgstr ""
```

And the third kind is the payoff for the two halves of this chapter meeting:

```text
#. module: librefleet
#: model:librefleet.service.type,name:librefleet.service_type_tire_rotation
msgid "Tire Rotation"
msgstr ""
```

That is the *value* of a record, exportable only because the field became `translate=True`
in step 6 **and** the record has a stable xml id from the master data you shipped in step
1. Data with no xml id has nothing for a `.po` to point at.

</Step>

<Step>
### Write the French translations

Write `i18n/fr.po` with three translations, keeping each `msgid` byte-identical to the
template (that string *is* the lookup key) and the named placeholder intact:

```text title="addons/librefleet/i18n/fr.po"
# Translation of Odoo Server.
# This file contains the translation of the following modules:
# 	* librefleet
#
msgid ""
msgstr ""
"Project-Id-Version: Odoo Server 19.0\n"
"Report-Msgid-Bugs-To: \n"
"MIME-Version: 1.0\n"
"Content-Type: text/plain; charset=UTF-8\n"
"Content-Transfer-Encoding: \n"
"Plural-Forms: nplurals=2; plural=n > 1;\n"

#. module: librefleet
#: model:librefleet.service.type,name:librefleet.service_type_tire_rotation
msgid "Tire Rotation"
msgstr "Rotation des pneus"

#. odoo-python
#: code:addons/librefleet/wizards/service_order_approve.py:0
msgid "Only an order that is in progress can be marked done."
msgstr "Seul un ordre en cours peut être marqué comme terminé."

#. odoo-python
#: code:addons/librefleet/wizards/service_order_approve.py:0
msgid ""
"This order loses money (margin %(margin).2f). A manager must tick the "
"override to complete it anyway."
msgstr ""
"Cet ordre est déficitaire (marge %(margin).2f). Un responsable doit cocher "
"la dérogation pour le terminer malgré tout."
```

The gettext header (that first `msgid ""` block) is boilerplate every `.po` file
carries; write it once and never think about it again. The third message is the one
with the named placeholder, `%(margin).2f`, sitting untouched in both `msgid` and
`msgstr`: the translator moves it wherever French phrasing wants it, but never renames
or drops it, since Python fills it in by name at raise time, not by position.

</Step>

<Step>
### Import it, and prove both halves landed

One command reads the file back in:

```console
$ docker compose exec odoo odoo i18n import -c /etc/odoo/odoo.conf -d tutorial -l fr /mnt/extra-addons/librefleet/i18n/fr.po
...
INFO ? odoo.tools.translate: translations are loaded successfully
```

Now check both halves of the chapter's translation table separately, because they landed
in two completely different places. First the **field value**, which went into the
database, next to the English one:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select name from librefleet_service_type where name->>'en_US'='Tire Rotation';"
```

```text
                           name
-----------------------------------------------------------
 {"en_US": "Tire Rotation", "fr_FR": "Rotation des pneus"}
```

And now the **code string**, which went nowhere near Postgres: it translates when the
wizard actually raises it, read from the in-memory catalog. Build the smallest thing that
trips the first message, in both languages:

```console
$ docker compose exec odoo odoo shell -c /etc/odoo/odoo.conf -d tutorial --no-http
>>> v = env["librefleet.vehicle"].create({"license_plate": "I18N-1"})
>>> o = env["librefleet.service.order"].create({"vehicle_id": v.id})
>>> W = env["librefleet.service.order.approve.wizard"]
>>> for lang in ("en_US", "fr_FR"):
...     try:
...         W.with_context(lang=lang).create({"order_id": o.id}).action_confirm()
...     except Exception as e:
...         print(f"{lang}: {e}")
...
en_US: Only an order that is in progress can be marked done.
fr_FR: Seul un ordre en cours peut être marqué comme terminé.
>>> env.cr.rollback()
```

One `_()` call, two languages, and the rollback throws away the throwaway records.

<Callout type="warn" title="Why the same string looks untranslated in the shell">
  Try that message on its own at the shell prompt and it comes back in English even though
  the translation is loaded:

  ```console
  >>> env(context={"lang": "fr_FR"})._("Only an order that is in progress can be marked done.")
  'Only an order that is in progress can be marked done.'
  ```

  Not a bug. `_()` works out *which module's* catalog to search from the file its caller
  lives in, and shell input belongs to no module, so there is nothing to search. Inside
  `wizards/service_order_approve.py` it resolves to `librefleet` and translates, which is
  exactly what the wizard call above proves. Test code translations by triggering the real
  code path, never by calling `_()` from the shell.
</Callout>

</Step>

</Steps>

## Verify

```console
$ python3 odoolings.py check ch34
✔ Tire Rotation shipped as noupdate master data
✔ a second noupdate record was added on a later upgrade
✔ the service type name is a translatable field
✔ French is installed
✔ Tire Rotation reads in French

ch34 complete! On to the next chapter 🔧
```

That covers your real, permanent tutorial database. The demo dataset needs its own
scratch database to prove anything (your real one never has demo loaded, by
design), so it's a second, separate check:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial_demo_check -i librefleet --with-demo --stop-after-init
$ python3 odoolings.py check ch34-demo --db tutorial_demo_check
✔ the demo partner exists
✔ the demo vehicle is owned by the demo partner
✔ the demo order's refs resolve to the vehicle and the master data

ch34-demo complete! On to the next chapter 🔧
```

```console
$ docker compose exec db psql -U odoo -d postgres -c "drop database tutorial_demo_check;"
```

## Gotchas

- **A CSV filename is a model lookup, not a label.** `res_partner_demo.csv` fails
  with `KeyError: 'res_partner_demo'`, a real error from this chapter's own first
  attempt. The model name goes before the first `-`; anything after it is yours.
- **CSV cannot opt out of noupdate.** There is no CSV equivalent of
  `<data noupdate="...">`. A `"data"`-listed CSV is always fully rewritable; a
  `"demo"`-listed CSV is always protected. If a CSV row needs the other behavior,
  it needs to become an XML record instead.
- **noupdate protects existing records, not the file.** Add a new record with a
  fresh xml id to an already-shipped noupdate block, and it is created on the very
  next upgrade, same as any other record. Only records that already exist by xml id
  are left alone.
- **A plain `-u` will not add demo data to a database that never had it.**
  `ir_module_module.demo` is set once, at that module's first install, and an
  upgrade only reloads demo if that flag is already true. Adding demo files later
  needs a fresh database with `--with-demo`, not an upgrade of an existing one.
- **Odoo 19 defaults to `--without-demo`.** If you expected demo data to just show
  up the way an older memory says it should, that default, not a bug in your
  files, is why it didn't.
- **`translate=True` changes the column type, so raw SQL has to change with it.**
  `where name = 'Tire Rotation'` stops working the moment the field becomes
  translatable, with `invalid input syntax for type json` rather than anything
  mentioning translations. Use `name->>'en_US'`. ORM domains are unaffected.
- **`-l` on the i18n commands is greedy.** It takes a list, so
  `i18n export -l pot librefleet` reads *both* `pot` and `librefleet` as language codes
  and then complains `the following arguments are required: MODULE`. Put the module name
  before the flag: `i18n export ... librefleet -l pot`.
- **Exporting without `-o` writes into the module folder, which fails in this setup.**
  The container runs as uid 100 and your bind-mounted `addons/` belongs to your host user,
  so the in-place write dies with `PermissionError`. Export to stdout with `-o -` and let
  your own shell create the file, which is the recipe used above.
- **Pass the iso_code to the i18n commands, not the locale code.** `-l fr_FR` works but
  logs `Ignoring not found languages: fr_FR` first, which reads like a failure and is not
  one: the lookup matches on either `code` or `iso_code`, then reports what is missing by
  comparing against `iso_code` only, and French is `code=fr_FR`, `iso_code=fr`. Passing
  `-l fr` does the same work without the scare.
- **Code translations are not in the database.** There is no table to inspect and nothing
  for a database backup to carry: they are read from your module's `i18n/*.po` files. Ship
  the `.po`, or the translation does not exist on the next machine.

## Quick check

<Quiz
  questions={[
    {
      q: 'A noupdate="1" record with xml id my_module.thing already exists in the database. You change its field values in the XML and run an upgrade. What happens to the field values already in Postgres?',
      options: [
        'they are overwritten with the new values from the XML, same as any other record',
        'they stay exactly as they were; noupdate skips writing to a record that already exists by that xml id',
        'the upgrade fails with an error, since noupdate records cannot be edited once created',
      ],
      answer: 1,
      why: 'Verified directly against the loader: _tag_record checks whether the xml id already resolves to a record, and if it does, stores its id and returns without writing anything. No error, no partial write, just a skip. The other two options both assume noupdate does something more dramatic than a plain skip, which is not what the code does.',
    },
    {
      q: 'You add a brand-new record with a fresh xml id to a noupdate="1" block that already shipped in an earlier version, then run an upgrade. What happens to the new record?',
      options: [
        'nothing happens: once a noupdate block has run once, no record inside it, old or newly added, can ever be created after that point',
        'it is created normally, exactly like any other record',
        'it is created, but flagged for manual review before a manager approves it in the UI',
      ],
      answer: 1,
      why: 'This is the exact experiment the chapter runs: Wheel Alignment, a new xml id in the same block as the already-existing Tire Rotation, was created on the same upgrade that left Tire Rotation completely untouched. noupdate is keyed to whether a specific xml id already exists in the database, not to the file or block as a whole, so a fresh xml id gets created same as it would without noupdate at all.',
    },
    {
      q: 'Your tutorial database has had librefleet installed since chapter 8, with no demo flag used. You add a "demo" key to the manifest today and run a plain upgrade. What shows up?',
      options: [
        'the demo records, since the manifest now declares them and an upgrade reloads every declared data source',
        'nothing at all',
        'a warning asking whether to enable demo data for this database, which you can accept interactively',
      ],
      answer: 1,
      why: 'Read directly from odoo/modules/loading.py: the upgrade branch checks package.demo, the value already stored in the database from the module\'s first install, not whether the current manifest happens to declare a demo key. Since librefleet installed without --with-demo back in chapter 8, that flag is false and stays false through any later upgrade; only a fresh install with --with-demo can set it.',
    },
    {
      q: 'Why does the demo service order use XML for one record instead of a third CSV file?',
      options: [
        'CSV cannot express a ref() to another record, and the order needs two: one to the vehicle, one to the master-data service type',
        'CSV files are limited to exactly one row, so a service order with more fields than the vehicle needed XML instead',
        'there is no real reason, XML and CSV are interchangeable for every case and this was an arbitrary choice',
      ],
      answer: 0,
      why: 'CSV can reference other records too, through a field_name:id column, which the vehicle demo file already does for owner_id. But a service order needing two separate refs (vehicle_id and service_type_id) to records in two different files is exactly the "shape varies, and there is only one of them" case Concepts describes as XML\'s territory, not a hard CSV limitation on ref count.',
    },
    {
      q: 'You restore a database backup onto a fresh machine, without the module source. Which translations survive?',
      options: [
        'Both kinds, since a database dump carries every translated term the system knows about',
        'The field values, but not the code strings',
        'Neither, because translations are rebuilt from the .po files on the next module upgrade anyway',
      ],
      answer: 1,
      why: 'The two halves live in different places, which is the whole point of the Concepts table. translate=True field values are jsonb columns, so they are inside the dump. Code strings marked with _() are read from the module i18n/*.po files on disk and cached in memory, never written to a table, so a database alone cannot carry them. This is also why ir.translation being gone matters: there is no table left for a backup to sweep up.',
    },
    {
      q: 'A field becomes translate=True and an existing psql query filtering on it starts failing. What does the error say, and why?',
      options: [
        'operator does not exist, because comparing jsonb against text needs an explicit cast that the query is missing',
        'invalid input syntax for type json, because Postgres tries to parse the compared text as JSON',
        'column does not exist, because Odoo renames the column when it becomes translatable',
      ],
      answer: 1,
      why: 'Verified by running it: the column is now jsonb, so Postgres attempts to read Tire Rotation as a JSON document and reports Token "Tire" is invalid. The fix is to reach into the language key, name->>\'en_US\'. Nothing is renamed, and the error never mentions translations, which is what makes it puzzling the first time. ORM domains keep working untouched, so only raw SQL is affected.',
    },
  ]}
/>

## Exercises

1. ⭐ Add a fourth master-data service type of your own (name, flat_fee,
   default_duration_h) to `data/service_type_master.xml`, upgrade, and confirm with
   `psql` that both your new record and the two already there are unaffected.
2. ⭐⭐ Extend the demo dataset with a second vehicle and a second service order for
   it, owned by a new demo partner. Chain every reference through `ref()` /
   `:id` columns rather than typing a raw database id anywhere, and prove it loads
   cleanly into a fresh scratch database.
3. ⭐⭐ Explain, from what this chapter verified in the loader source, why renaming
   an existing noupdate xml id in your XML (keeping everything else the same) is
   effectively the same as creating a brand-new record on the next upgrade, rather
   than renaming the old one in place.
4. ⭐⭐ Translate the two remaining wizard strings (the field labels
   `Complete anyway (negative margin)` and `Completion Note`) into French. Neither needs
   `_()`, so work out from step 9's three entry kinds which comment each one carries in the
   `.pot`, add them to `fr.po`, re-import, and confirm in the UI with French selected.
5. ⭐⭐ Add a second language of your choice with `i18n loadlang`, then read
   `librefleet_service_type.name` in psql. Explain why one column can now serve three
   languages without a schema change, and what that costs compared with a separate
   translations table.
6. ⭐⭐⭐ Break-it lab: rename `data/res.partner-demo.csv` to
   `data/res_partner-demo.csv` (an underscore instead of a dot before the dash),
   updating the manifest's `"demo"` entry to match so Odoo still finds the file.
   Install into a fresh scratch database with `--with-demo` and read the actual
   error: `KeyError: 'res_partner'`, not the `FileNotFoundError` you'd get if you
   forgot to update the manifest too. Explain, from the filename rule in Concepts,
   exactly which model Odoo went looking for and why that specific string doesn't
   resolve, before fixing both the filename and the manifest back.

## Further reading

- [Translating modules (19.0)](https://www.odoo.com/documentation/19.0/developer/howtos/translations.html):
  the export/translate/import loop from the official side, including the web editor route
  this chapter skipped in favour of the CLI.
- [Data files reference (19.0)](https://www.odoo.com/documentation/19.0/developer/reference/backend/data.html):
  the full `<record>`/`<field>` grammar, `eval`, `search`, and function-call records
  this chapter only used a slice of.
- [External identifiers (19.0)](https://www.odoo.com/documentation/19.0/developer/reference/backend/data.html#external-identifiers):
  more on how xml ids are namespaced across modules.
- [`account.account-kw.csv` in core](https://github.com/odoo/odoo/blob/19.0/addons/l10n_kw/data/template/account.account-kw.csv):
  a real dash-suffixed CSV filename from Odoo's own localization modules.
