---
title: "1. What Odoo Is"
description: ERP concepts, apps vs modules, Community vs Enterprise, and Odoo's yearly release rhythm.
---

**Goal:** understand what Odoo is as a product and a platform, so every technical
decision later makes sense. · **Time:** ~1 h · **Checkpoint:** none, Part 0 has no code.

## Why this matters

You're about to spend weeks learning a framework. Before touching code, you need to
know what the thing actually *is*, because Odoo's architecture only makes sense once
you understand its business model: one company selling a suite of business apps that
thousands of other companies customize for their clients. Nearly every design choice
you'll meet later (modules, inheritance everywhere, views stored as data) exists to
make that customization industry possible.

## Concepts

### ERP, in one paragraph

An ERP (Enterprise Resource Planning) system is the software a company runs its
operations on: sales, invoicing, inventory, accounting, HR, all sharing **one
database**. A confirmed sale automatically reserves stock, creates an invoice, and
shows up in the accounting reports. The alternative is a zoo of disconnected tools glued
together with spreadsheets and re-typing. The single shared database is the entire value
proposition.

<Mermaid
  label="Disconnected tools re-typing data into each other, versus apps sharing one database"
  chart={`flowchart LR
    subgraph before["Without an ERP"]
      direction TB
      B1["Sales<br/>spreadsheet"]
      B2["Stock<br/>spreadsheet"]
      B3["Accounting<br/>package"]
      B1 -. "re-typed by hand" .-> B2
      B2 -. "re-typed by hand" .-> B3
    end
    subgraph after["With an ERP"]
      direction TB
      A1["Sales"]
      A2["Inventory"]
      A3["Accounting"]
      DB[("one shared<br/>PostgreSQL database")]
      A1 --> DB
      A2 --> DB
      A3 --> DB
      DB --> R["reports<br/>every app reads the same numbers"]
    end
    before ~~~ after`}
/>

Read the left side as the reason the right side exists. Every arrow on the left is a
person re-typing something, and every one of them is a chance to be wrong. On the right
there is nothing to re-type, because there is only one copy of the data.

### Odoo's take on it

Odoo is a suite of open-source business apps (CRM, Sales, Inventory, Accounting,
Website, Manufacturing, HR, and dozens more) built on one shared framework and one
PostgreSQL database. Two things make it different from classical ERPs:

1. **It's modular to the bone.** Every app is a *module*, and so is every
   customization. Installing "Sales" and installing a 200-line extension you wrote
   yourself use exactly the same mechanism.
2. **The framework is a product too.** You get a full web framework (ORM, views,
   reports, access control, web client) that you'll use to build things that look and
   feel native. That framework is what this tutorial teaches.

### Apps vs modules

There is no technical difference. An **app** is just a module with
`'application': True` in its manifest, which makes it show up on the Apps home screen
with an icon. Modules that extend other modules (most of what you'll write) don't set
that flag. When Odoo people say *addon*, they also mean module. You'll see
`--addons-path` as the flag that tells the server where to find them.

<Mermaid
  label="One flag in the manifest is the only thing separating an app from a module"
  chart={`flowchart LR
    M["a module<br/>a directory with __manifest__.py"] --> Q{"'application': True<br/>in the manifest?"}
    Q -- "yes" --> A["an app<br/>gets an icon on the Apps screen"]
    Q -- "no" --> B["still a module<br/>installs and runs identically"]`}
/>

That single flag is the whole distinction. Both branches install the same way, load the
same way and can extend anything; one of them just gets an icon. Keep this in mind the
first time someone tells you a customization needs to be "a proper app".

### Community vs Enterprise

Odoo comes in two editions:

- **Community** is open source (LGPL-3): the complete framework and core apps. Its
  source lives publicly at [github.com/odoo/odoo](https://github.com/odoo/odoo). This
  is what the OCA targets and what this tutorial uses.
- **Enterprise** is a paid subscription that adds apps and features on top of Community
  (Studio, full Accounting localizations, Field Service, upgrade service, mobile
  apps...). Its source is visible to partners and customers but is **not** freely
  redistributable.

<Mermaid
  label="Enterprise contains Community: the same framework with more modules on top"
  chart={`flowchart TB
    subgraph ent["Enterprise, a paid subscription"]
      direction TB
      subgraph com["Community, LGPL-3, github.com/odoo/odoo"]
        direction TB
        C1["the framework<br/>ORM, views, reports, access control, web client"]
        C2["the core apps<br/>CRM, Sales, Inventory, Accounting, Manufacturing..."]
      end
      E1["Studio, accounting localizations, Field Service,<br/>upgrade service, mobile apps"]
    end`}
/>

<Callout title="Enterprise is a superset">
  Enterprise *contains* Community, which is why the boxes nest rather than sit side by
  side. It's the same framework with more modules on top, so everything you learn here
  transfers directly to Enterprise projects. We'll flag Enterprise-only features in
  boxes like this when they matter.
</Callout>

### The version rhythm: there is no LTS

Odoo ships **one major version every year, around October**, announced at the Odoo
Experience conference in Brussels. Only the **three most recent** majors receive
support and security fixes. As this is written (mid-2026), that window is **17, 18
and 19**, with Odoo 20 expected in October 2026, at which point 17 falls out.

<Mermaid
  wide
  label="Support windows for Odoo 17 to 20, showing the three-version sliding window"
  chart={`%%{init: {'gantt': {'useMaxWidth': false, 'barHeight': 30, 'barGap': 10, 'fontSize': 16, 'sectionFontSize': 16, 'leftPadding': 80, 'topPadding': 50}}}%%
gantt
    title One major each October, only the newest three supported
    dateFormat YYYY-MM
    axisFormat %Y
    tickInterval 1year
    section 17.0
      supported          :done, 2023-10, 2026-10
    section 18.0
      supported          :active, 2024-10, 2027-10
    section 19.0
      supported, this tutorial :active, 2025-10, 2028-10
    section 20.0
      expected Oct 2026  :2026-10, 2029-10`}
/>

Read it as a window three bars tall sliding to the right one notch a year. The moment
20.0 appears at the bottom, 17.0 drops off the top: that is the same event, not two.

This has two consequences you'll feel throughout your career:

- Clients must **migrate** roughly every three years, whether they like it or not.
  Migration work is a big part of integrator life, and chapter 47 is dedicated to it.
- Everything is **branch-per-version**: the Odoo source, the docs, OCA repositories,
  and your own projects all have `17.0`, `18.0`, `19.0` branches. A module written for
  one version does not run unmodified on the next.

This tutorial targets **Odoo 19.0 Community**. Where 18 behaves differently, you'll
see an "On Odoo 18" callout, useful when your client project runs a version behind.

### Where Odoo runs

Three hosting models, and the choice constrains what you can customize:

| Hosting model | What it is | Custom modules? |
|---|---|---|
| **Odoo Online** | SaaS at odoo.com, Enterprise only | No. Configuration and Studio only |
| **odoo.sh** | Odoo's PaaS: git-driven, per-branch staging builds | Yes |
| **On-premise / self-hosted** | You run the server (bare metal, VMs, Docker, k8s) | Yes, full control |

Integrators mostly live in the last two. This tutorial self-hosts with Docker, which
is also the closest to how professional teams run client projects.

## Hands-on

No code yet, but do actually open these. The goal is that none of these sites feel
foreign by the end of Part 0.

1. Skim the [Odoo apps overview](https://www.odoo.com/page/all-apps) to see the
   breadth of the suite. Count how many of these you'd expect a small workshop
   business to use.
2. Open [github.com/odoo/odoo](https://github.com/odoo/odoo) and switch the branch
   dropdown between `17.0`, `18.0` and `19.0`. This is the branch-per-version model
   you'll use everywhere.
3. Open the [Odoo 19 release notes](https://www.odoo.com/odoo-19-release-notes) and
   skim what a yearly major brings.

## Verify

You're done when you can answer, without looking: what's the difference between an app
and a module? Which edition does this tutorial use and why? When does an Odoo version
stop being supported? Which hosting model forbids custom modules?

(That's exactly what the quiz below checks.)

## Gotchas

- **Odoo versions are not semver.** "19.0" is a product generation, not a
  major.minor you can partially upgrade across. Data and code migrate between
  generations via explicit migration work.
- **Community is not a demo.** It's the complete framework. Some of the largest Odoo
  installations run pure Community plus OCA modules.
- **"Odoo" the company vs "Odoo" the software.** The company (Odoo SA, Belgium) builds
  the software and sells Enterprise/hosting. The next chapter maps the whole ecosystem
  around it.

## Quick check

<Quiz
  questions={[
    {
      q: 'Odoo ships a new major version...',
      options: [
        'whenever enough features accumulate',
        'once a year, around October',
        'every LTS cycle, like Ubuntu',
      ],
      answer: 1,
      why: 'One major per year, announced at Odoo Experience, and only the three most recent majors are supported. There is no LTS.',
    },
    {
      q: 'An Odoo "app" is...',
      options: [
        "just a module with 'application': True in its manifest",
        'a separate binary installed next to the server',
        'an Enterprise-only concept',
      ],
      answer: 0,
      why: 'Apps and modules are the same mechanism; apps are simply modules flagged to appear on the Apps home screen.',
    },
    {
      q: 'Your client wants a custom module AND wants Odoo to host everything. Which options work?',
      options: [
        'Odoo Online or odoo.sh',
        'Only odoo.sh',
        'Only on-premise',
      ],
      answer: 1,
      why: 'Odoo Online (SaaS) does not allow custom modules. odoo.sh is Odoo-hosted and git-driven, so custom code is fine there.',
    },
  ]}
/>

## Exercises

1. ⭐ Find out which Odoo version a company you know (or your employer's clients) runs,
   and work out when it falls out of support.
2. ⭐ Pick one app from the Odoo suite and find its module directory in the
   `19.0` branch of `odoo/odoo` on GitHub (hint: everything lives under `addons/`).

## Further reading

- <Icon name="docs" /> [Official docs home (19.0)](https://www.odoo.com/documentation/19.0/):
  bookmark it, we link into it constantly.
- <Icon name="docs" /> [Editions comparison](https://www.odoo.com/page/editions): the
  official Community/Enterprise feature matrix.
- <Icon name="docs" /> [Odoo release notes](https://www.odoo.com/page/release-notes): one
  page per yearly release.
