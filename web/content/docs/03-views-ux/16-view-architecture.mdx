---
title: "16. View Architecture"
description: Views are records, inheritance patches them at render time, and priority decides which one you see.
---

**Goal:** understand exactly how Odoo assembles the UI it shows you, and modify a view
without touching its source. · **Time:** ~2 h · **Checkpoint:** `code/checkpoints/ch16`

## Why this matters

**View inheritance is the single most-used skill in professional Odoo work.** Client
wants a field on the sales order form? Extension view. Hide a button for one group?
Extension view. Nearly every integrator ticket that says "can we see X on the Y screen"
resolves to twenty lines of the XML you'll write in this chapter.

Chapter 3 planted the idea that the UI is data, and Part 2 had you writing views as XML
records without dwelling on what happens to them. Now it becomes the whole subject.
Part 4 extends core apps with exactly this mechanism; here you learn it safely inside
LibreFleet, including how it breaks.

Two words in this chapter are going to fight you, so deal with them up front.

The first is **view**. You have been writing views as XML files, and a file feels like
source code: something the program reads and obeys. It is not. The file is a loader, and
what it loads is a row in a table.

The second is **inheritance**, which here means almost nothing it means in Python. A
Python subclass gets its own copy of the parent's behaviour and the parent carries on
untouched. An Odoo extension view is a *list of edits* aimed at somebody else's XML, and
the screen you end up looking at exists nowhere until the moment you ask for it. Same
word, different machine.

So here is the picture to hold, before any attribute names arrive: **Odoo does not store
the screen you see.** It stores one base drawing, plus separate little lists of edits, and
it redraws the screen from scratch on every single request.

## Concepts

### A view is a row in ir.ui.view

Every list, form, kanban and search screen is described by a record of `ir.ui.view`,
holding among other things: the `model` it renders, its `type` (list, form...), its
<Term k="arch (view architecture)">`arch`</Term> (the XML you've been writing), a `priority`, a `mode`, and optionally an
`inherit_id`. Your module's XML files don't "declare" views in some special sense;
they just load records into this table, like any other data file.

**The file on disk is not the view.** It is a script that inserts a row. Once the row
exists, Odoo never reads your file again; it renders whatever the table holds.

Two things at the end of this chapter fall straight out of that. View changes need an
*upgrade* rather than a restart, because the upgrade is what rewrites the row. And anyone
with the right access can edit a view in the interface and quietly diverge from your file,
because the interface and your file write to the same place.

Look at LibreFleet's own rows (this is your database, after chapter 15):

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select id, name, type, priority, mode, inherit_id from ir_ui_view where model = 'librefleet.vehicle' order by id;"
 id  |          name           | type | priority |  mode   | inherit_id
-----+-------------------------+------+----------+---------+------------
 253 | librefleet.vehicle.list | list |       16 | primary |
 254 | librefleet.vehicle.form | form |       16 | primary |
(2 rows)
```

Two views, both `primary`, both at the default priority of 16. `mode` has exactly two
values: `primary` means "a view in its own right", and `extension` means "a bundle of
patches applied on top of another view".

You never type `mode` yourself. Odoo derives it when the record is created: a view with
an `inherit_id` becomes an `extension`, one without stays `primary`. `priority` is a plain
integer that defaults to 16. Those two columns are this chapter's levers.

The two modes are the drawing and the edit list, in table form:

| | `primary` | `extension` |
|---|---|---|
| What it is | a drawing in its own right | a list of edits to somebody's drawing |
| Has an `inherit_id`? | usually not | always |
| Competes for the default slot? | yes, by `priority` | no, it is never picked directly |
| How you get one | leave `inherit_id` off (or set `mode` yourself, the rarer cousin below) | set `inherit_id` |

### How Odoo picks and builds the view you see

Nothing about the finished screen is stored anywhere. Every time a client asks for one,
Odoo chooses a starting drawing, gathers the edit lists that claim to modify it, applies
them in order, hands the result over, and throws it away.

When the web client asks for, say, the form view of `librefleet.vehicle`, the server:

<Mermaid
  label="How Odoo assembles a form view: pick the lowest-priority primary view, collect its extensions, and apply their patches in priority order"
  chart={`flowchart TD
    A["client requests form view<br/>(no explicit view_id)"] --> B["pick the PRIMARY view of that<br/>model and type with the<br/>LOWEST priority"]
    B --> C["collect its extension views<br/>(mode=extension, inherit_id set)"]
    C --> D["apply each extension's patches<br/>to the arch, in priority order"]
    D --> E["combined arch sent to the client"]`}
/>

Two consequences worth engraving:

- **<Term k="extension view">Extensions</Term> never replace, they patch.** An extension view is a list of edits
  (add this after that, change this attribute) applied to its parent's arch at
  request time. The parent's record is untouched; uninstall the extension and the
  original is back, unchanged.
- **<Term k="view priority">Priority</Term> is a golf score.** Among <Term k="primary view">primary views</Term>, the *lowest* number wins the
  default slot. Base views sit at 16, so a view with priority 8 hijacks the default,
  and one at 99 never wins the contest. A losing view is still reachable: a window
  action (chapter 11) has a `view_id` field pointing at one specific `ir.ui.view`
  record. Set that field and the action opens that view instead of the priority winner.

### The patch language

An extension view's `arch` contains locator elements, each wrapping the nodes to
insert (or the attributes to change):

```xml
<xpath expr="//field[@name='service_count']" position="after">
    <field name="active"/>
</xpath>
```

XPath is a small query language for pointing at a node inside an XML document. This
`expr` reads left to right: `//` means "anywhere below the root, at any depth", `field`
is the tag to look for, and `[@name='service_count']` keeps only the tags whose `name`
attribute is `service_count`. Put together: the `service_count` field, wherever it sits
in the parent's form.

Odoo evaluates the expression against the parent's arch and takes the first node it
finds. `position` says what to do there:

| position | Effect |
|---|---|
| `after` / `before` | insert your nodes as siblings of the match |
| `inside` | append your nodes as children of the match (the default) |
| `replace` | remove the match, put your nodes in its place |
| `attributes` | keep the node, change only attributes via `<attribute>` children |

For the common case of locating a field by name, there's a shorthand that reads
better than XPath and does the same thing:

```xml
<field name="mileage_km" position="attributes">
    <attribute name="string">Odometer (km)</attribute>
</field>
```

Any locator that matches nothing is a hard error at load time, and that's a feature:
you find out at upgrade, not silently in production. You'll trigger it on purpose
below.

<Callout type="warn" title="Gotcha">
  Use `position="replace"` as the last resort. Every extension that replaces a node
  deletes it for all other extensions too, including ones installed later by modules
  you've never heard of. Integrators have a name for the resulting support tickets:
  "module A broke module B". Prefer `attributes` (with `invisible="1"`) to hide
  things, and keep replace for nodes your own module created.
</Callout>

### Primary inheritance, the other mode

`mode="primary"` with an `inherit_id` is the rarer cousin. It produces a *new standalone
view*: it starts from a copy of the parent, applies patches, and leaves the parent's own
rendering alone. Core uses it when one particular action needs a specialized flavor of a
form. You'll recognize it when you meet it in core. For day-to-day extension work, the
default `extension` mode is what you want.

## Hands-on

The reception desk has two requests: show the archive toggle on the vehicle form
(chapter 15 taught archiving, but there's no way to do it from the UI yet), and call
the mileage field what everyone calls it, the odometer. You'll do both without
touching `vehicle_views.xml`.

<Steps>

<Step>
### Write the extension view

Create `views/vehicle_views_inherit.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_librefleet_vehicle_form_reception" model="ir.ui.view">
        <field name="name">librefleet.vehicle.form.reception</field>
        <field name="model">librefleet.vehicle</field>
        <field name="inherit_id" ref="view_librefleet_vehicle_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='service_count']" position="after">
                <field name="active"/>
            </xpath>
            <field name="mileage_km" position="attributes">
                <attribute name="string">Odometer (km)</attribute>
            </field>
        </field>
    </record>

</odoo>
```

`inherit_id` points at the base form through its XML id (same-module `ref`, so no
`librefleet.` prefix needed). That alone makes this an extension view.

The arch holds two patches: an XPath locator inserting `active` after the service
counter, and a shorthand locator relabeling `mileage_km` for this form only. The field
itself, and every other view, keeps its model-level label.

Two edits in `__manifest__.py`. First, add the new file to `data`, right after
`vehicle_views.xml`, so the base form record already exists when Odoo resolves this
file's `ref`:

```python
        "views/vehicle_views.xml",
        "views/vehicle_views_inherit.xml",
```

Second, bump the version. This one is an **edit, not an addition**. The `version` line
already exists near the top of the manifest, between `summary` and `category`, and you
are changing its value. Coming from chapter 15 it currently reads `19.0.1.8.0`, so raise
the third number by one:

```python title="addons/librefleet/__manifest__.py"
    "version": "19.0.1.9.0",     # was: 19.0.1.8.0
```

New views are a feature rather than a fix, which is why the minor number moves and not
the patch (chapter 8 set out the `<odoo major>.<module major>.<module minor>.<patch>`
scheme). If your number differs because you did the exercises, that is fine: what matters
is that it goes up.

<Callout type="info" title="The upgrade does not depend on this">
  Worth knowing so you never chase a phantom bug: `-u librefleet` reloads your data files
  whether or not you touched the version, exactly as chapter 18's step 4 says. Bumping is
  a habit for other people's benefit, not a switch that makes the next command work.

  The number really drives two things: *Update Apps List*, which compares the manifest on
  disk against the `latest_version` chapter 8 read out of `ir_module_module`, and the
  deployment tooling that decides which modules changed. Skip it and this chapter still
  works; skip it on a real project and nobody can tell your releases apart.
</Callout>

Then upgrade:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
```

A clean run ends with `Modules loaded.` and no traceback. If instead it stops with this,
do not read past it:

```text
odoo.tools.convert.ParseError: while parsing /mnt/extra-addons/librefleet/views/vehicle_views_inherit.xml:7
Error while parsing or validating view:
Element '<xpath expr="//field[@name='service_count']">' cannot be located in parent view
```

That means `service_count` is not on the vehicle form in `views/vehicle_views.xml`, so the
xpath has nothing to attach to. Chapter 13 put it in the "Usage" group, after
`mileage_km`; add it there and upgrade again.

Catch this now, because the failure is *file-wide*. Both records in
`vehicle_views_inherit.xml` are abandoned, so the step-2 query below comes back with two
rows instead of four, and nothing on screen says why. You are meeting this chapter's
break-it lab four steps early, by accident.
</Step>

<Step>
### Add the competing list view

Reception also wants a stripped-down vehicle list. In the same file, before
`</odoo>`, add a *second standalone list view* for the model:

```xml
    <record id="view_librefleet_vehicle_list_reception" model="ir.ui.view">
        <field name="name">librefleet.vehicle.list.reception</field>
        <field name="model">librefleet.vehicle</field>
        <field name="priority" eval="99"/>
        <field name="arch" type="xml">
            <list>
                <field name="license_plate"/>
                <field name="owner_id"/>
                <field name="service_count"/>
            </list>
        </field>
    </record>
```

No `inherit_id`, so this is a full view in its own right, competing for the default slot
through priority. `eval="99"` is how that number gets set: `eval` runs the attribute as
Python, so the field receives the integer 99 rather than text.

At 99 the view deliberately loses to the original's 16, and that is the point. Nobody's
Vehicles menu changes today. You gain a second primary view to run step 4's experiment
against, and it matches how real modules ship an alternative screen: parked at a high
priority, opened only by an action that names it with `view_id`.

It lives in the same already-registered file, so no new manifest entry and no version
bump, just:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
```

Then check the table:

```console
$ docker compose exec db psql -U odoo -d tutorial -c "select id, name, type, priority, mode, inherit_id from ir_ui_view where model = 'librefleet.vehicle' order by id;"
 id  |               name                | type | priority |   mode    | inherit_id
-----+-----------------------------------+------+----------+-----------+------------
 253 | librefleet.vehicle.list           | list |       16 | primary   |
 254 | librefleet.vehicle.form           | form |       16 | primary   |
 261 | librefleet.vehicle.form.reception | form |       16 | extension |        254
 262 | librefleet.vehicle.list.reception | list |       99 | primary   |
(4 rows)
```

Everything this chapter claimed, visible in one query: the extension with its
`inherit_id`, the standalone at priority 99.
</Step>

<Step>
### Watch the combination happen

<Term k="get_view">`get_view`</Term> is the method the web client itself calls, and it hands back a dict
whose `arch` key holds the finished XML. Open a shell against the tutorial database:

```console
$ docker compose exec odoo odoo shell -c /etc/odoo/odoo.conf -d tutorial --no-http
```

Then ask it for the combined form arch:

```console
>>> arch = env["librefleet.vehicle"].get_view(view_type="form")["arch"]
>>> 'name="active"' in arch
True
>>> import re
>>> re.search(r'<field name="mileage_km"[^>]*>', arch).group(0)
'<field name="mileage_km" string="Odometer (km)"/>'
```

The base record's arch in the database still has neither change (check it in the psql
`arch_db` column if you're curious); the merge happened at request time. That's why
this mechanism is safe: nothing was edited, something was *composed*.

**The sentence to carry out of this chapter:** Odoo does not keep the screen, it keeps the
ingredients and cooks it to order. That is why your patch cannot damage the base view, why
uninstalling your module leaves the original behind with nothing to clean up, and why Part
4 can reach into core apps you do not own without ever editing them.
</Step>

<Step>
### Rig the priority contest, then unrig it

Still in the shell, make the reception list win and watch the default flip live. The
tell is `service_count`: the base list shows `license_plate`, `owner_id`, `model_name`,
`year` and `mileage_km`, and only reception's list shows `service_count`, so whether
that name turns up in the combined arch tells you which of the two views won:

```console
>>> Vehicle = env["librefleet.vehicle"]
>>> "service_count" in Vehicle.get_view(view_type="list")["arch"]
False
>>> reception = env.ref("librefleet.view_librefleet_vehicle_list_reception")
>>> reception.priority = 8
>>> "service_count" in Vehicle.get_view(view_type="list")["arch"]
True
>>> reception.priority = 99
>>> "service_count" in Vehicle.get_view(view_type="list")["arch"]
False
>>> env.cr.rollback()
```

Priority 8 beats 16, so every plain vehicles action in the whole database would now
open reception's list; back at 99, order is restored. No file changed, no upgrade ran:
priority is data, and the winner is recomputed on every request. (The rollback keeps
your database matching the checkpoint; the file's `eval="99"` is the state that
ships.)
</Step>

<Step>
<BreakIt />
### Break-it lab: the locator that matches nothing

Sabotage the xpath in your extension view, replacing `service_count` with a field
that doesn't exist:

```xml
<xpath expr="//field[@name='does_not_exist']" position="after">
```

Upgrade and read the failure carefully:

```console
$ docker compose exec odoo odoo -c /etc/odoo/odoo.conf -d tutorial -u librefleet --stop-after-init
...
odoo.tools.convert.ParseError: while parsing /mnt/extra-addons/librefleet/views/vehicle_views.xml:18
Error while parsing or validating view:

Element '<xpath expr="//field[@name=&#39;does_not_exist&#39;]">' cannot be located in parent view

View error context:
{'file': '/mnt/extra-addons/librefleet/views/vehicle_views.xml',
 'line': 1,
 'name': 'librefleet.vehicle.form.reception',
 'view': ir.ui.view(259,),
 'view.model': 'librefleet.vehicle',
 'view.parent': ir.ui.view(184,),
 'xmlid': 'view_librefleet_vehicle_form'}
```

Your ids will not match this transcript. Postgres hands them out per database as records
are created, so read the names rather than the numbers.

**The `file` and `line` here lie to you a little.** They point at `vehicle_views.xml`,
the *base* form's file, not `vehicle_views_inherit.xml` where your broken xpath actually
lives. The base form isn't `noupdate`, so every upgrade rewrites its record. That write
is what triggers Odoo to recombine it with every extension and validate the result, while
`vehicle_views.xml` is still the file the loader is nominally "in".

The `name`, `view` and `view.parent` keys don't lie: `name` is your extension's own name,
`view` is its record id, and `view.parent` is the base form it failed to patch. When file
and line point somewhere innocent-looking, trust those three instead. They name the real
culprit every time.

You will meet this shape again whenever a version migration renames a field your xpath
anchored on (chapter 47's bread and butter). Restore `service_count`, upgrade, and
confirm the module loads clean.
</Step>

</Steps>

## Verify

```console
$ python3 odoolings.py check ch16
✔ the reception form extension exists and is an extension view
✔ the combined form arch contains your changes
✔ the reception list exists, standalone, priority 99
✔ the original list is still the default

ch16 complete! On to the next chapter 🔧
```

The second check calls `get_view` over RPC, exactly like the web client would, and
inspects the *combined* arch, so it proves your patches apply, not merely that a
record exists. For the eyeball version: open a vehicle in the browser, find the
archive toggle and the "Odometer (km)" label, and note the vehicles list looks
unchanged.

![The vehicle form showing the Odometer (km) relabeled field and the Active archive toggle, both added by the extension view without touching vehicle_views.xml](/screens/ch16-vehicle-form-reception.jpg)

## Gotchas

- **Your xpath is a contract with the parent's structure.** Anchor on stable,
  semantic nodes (a named field) rather than positional paths (`/form/sheet/group[2]`).
  The parent WILL be reorganized someday, and positional paths break silently or,
  worse, match the wrong node.
- **Only the first match is patched.** An `expr` matching several nodes applies to
  the first one. If a field appears twice in a form (it happens: once visible, once
  invisible), be more specific or you'll edit the wrong occurrence.
- **`replace` is a shared-space hazard.** You delete that node for every other
  extension too. Hide with attributes instead; replace only what your module owns.
- **Upgrade, not restart, for view changes.** Views are data, so `-u librefleet`
  reloads them and the running server serves the new combination immediately, the
  exact mirror image of Python changes (restart, no upgrade) from chapter 14.
- **A view can be edited in the database and diverge from your file.** Settings →
  Technical → Views lets anyone edit arch live. The next upgrade overwrites it with
  your file (views are not `noupdate`), which surprises whoever "fixed" it in the UI.
  In the field: never hot-edit views in a client database; it always ends in a diff
  nobody can explain.

## Quick check

<Quiz
  questions={[
    {
      q: 'A client module and a third-party module both extend the same core form. The third-party one uses position="replace" on the notes field your client edit anchors on. What happens?',
      options: [
        'both apply, because Odoo merges competing extensions by priority and then by module',
        'your xpath stops matching, since the node is gone, and view loading fails outright',
        'the higher-priority extension wins and the other is skipped without any warning',
      ],
      answer: 1,
      why: 'Extensions apply as sequential patches to one shared arch. Once replace removes the node, any later locator anchored on it matches nothing, and a non-matching locator is a hard load error. This is why replace is treated as a hazard.',
    },
    {
      q: 'You want your custom list view to become what users see when they open the standard menu entry. The base list has priority 16. You…',
      options: [
        'give your standalone list a priority below 16',
        'give it a priority above 16',
        'delete the base list view record',
      ],
      answer: 0,
      why: 'Lowest priority wins the default slot among primary views. Below 16 hijacks the default; above it, your view only appears when an action requests it by view_id. Deleting core records is never the answer.',
    },
    {
      q: 'An extension view relabels mileage_km to "Odometer (km)". Where does the change physically live?',
      options: [
        'the base view record is rewritten during upgrade',
        "the field's string attribute is updated on the model",
        'in the extension record; Odoo combines it with the base view at request time',
      ],
      answer: 2,
      why: 'Inheritance is composition, not modification. The base arch and the model field are untouched; get_view assembles parent plus patches per request, which is also why uninstalling the extension cleanly restores the original.',
    },
    {
      q: 'After editing a view XML file, your change does not show in the browser. Most likely you forgot to…',
      options: [
        'restart the Odoo server',
        'upgrade the module so Odoo reloads the view into ir.ui.view',
        'clear the browser cache',
      ],
      answer: 1,
      why: 'The file on disk is only read during install or upgrade; the server renders whatever ir.ui.view holds. -u librefleet reloads it, no restart needed (that is the Python side, chapter 14).',
    },
  ]}
/>

## Exercises

1. ⭐ The reception form should also show the `vin` right after `license_plate` in
   the *list* view. Extend the original list (not the reception one) with an xpath
   locator. Prove it with `get_view` in the shell.
2. ⭐⭐ Managers complain the Notes group wastes space for vehicles that have no
   notes. Using only `position="attributes"`, make the whole Notes group invisible
   when `notes` is empty (hint: the `invisible` attribute takes a Python-ish
   expression over field values). No new views allowed.
3. ⭐⭐ Create a "workshop floor" form view for vehicles as a *primary* view with
   `inherit_id` (mode="primary"): a copy of the base form that drops the Notes group
   entirely for a hypothetical wall-mounted screen. Confirm the default form is
   untouched and your variant renders via an action with an explicit `view_id`.
4. ⭐⭐⭐ Read the arch of a core view: in the shell, `get_view` the form of
   `res.partner` and count how many extension views contributed to it
   (`env["ir.ui.view"].search_count([("inherit_id", "=", id), ("mode", "=", "extension")])`
   on its primary form). Then skim two of those extension archs and identify which
   position each patch uses. Core is the best xpath phrasebook you own.

## Further reading

- [View records and inheritance reference (19.0)](https://www.odoo.com/documentation/19.0/developer/reference/user_interface/view_records.html):
  every locator, position, and resolution rule.
- [Server framework 101, chapter 12: inheritance (19.0)](https://www.odoo.com/documentation/19.0/developer/tutorials/server_framework_101/12_inheritance.html):
  the official walkthrough of the same mechanism.
- [View architectures reference (19.0)](https://www.odoo.com/documentation/19.0/developer/reference/user_interface/view_architectures.html):
  what's allowed inside each view type's arch, the map for chapters 17–19.
